<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="navigatingTo" class="page">
    <Page.actionBar>
        <ActionBar title="appwrite" icon="" class="action-bar">
        </ActionBar>
    </Page.actionBar>
    <StackLayout class="p-20">
        <ScrollView class="h-full">
          <StackLayout>
            <Button text="Create Account" tap="{{ demoCreateAccount }}" class="btn btn-primary rounded"/>
            <Button text="Login" tap="{{ demoLogin }}" class="btn btn-primary rounded"/>
            <Button text="Logout" tap="{{ demoLogout }}" class="btn btn-primary rounded"/>

          </StackLayout>
        </ScrollView>
    </StackLayout>
</Page>